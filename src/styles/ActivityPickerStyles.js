import { StyleSheet } from 'react-native'

const activityPickerStyles = StyleSheet.create({
    //Estilo de la pantalla de seleccion de actividad
    viewAP: {
      flex: 1,
      backgroundColor: '#fff',
      justifyContent: 'center',
      margin: 10,
    }
  });

export const activityPickerView = activityPickerStyles.viewAP
export default activityPickerStyles