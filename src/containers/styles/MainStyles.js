import { StyleSheet } from 'react-native'

const mainStyles = StyleSheet.create({
    //Estilo de la pantalla Main
    mainView: {
      flex: 1,
      margin: 10,
      backgroundColor: '#fff',
      justifyContent: 'center',
    },
  });

export const viewStyle = mainStyles.mainView
export default mainStyles
